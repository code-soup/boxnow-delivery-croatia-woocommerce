# Files to exclude from production
# Applied after production-files are copied
# One file/directory per line

# Vendor binaries (not needed in production)
vendor/bin

# Composer metadata files
vendor/composer/installed.json
vendor/composer/installers

# Test directories (if any exist in vendor)
vendor/*/tests
vendor/*/test

# Development-only files
.editorconfig
phpcs.xml
